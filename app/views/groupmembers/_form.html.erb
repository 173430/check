<%= form_with(model: groupmember, local: true) do |form| %>
  <% if groupmember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(groupmember.errors.count, "error") %> prohibited this groupmember from being saved:</h2>

      <ul>
      <% groupmember.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.number_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
