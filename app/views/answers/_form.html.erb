<%= form_with(model: answer, local: true) do |form| %>
  <% if answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--
  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>
  -->

  <div class="field">
    <%= form.label :質問 %>
    <%= form.select :question_id, Question.all.map{|o| [o.question, o.id]} %>
  </div>

  <div class="field">
    <%= form.label :返信 %>
    <%= form.text_field :answer %>
  </div>

  <div class="field">
    <%= form.label :画像 %>
    <%= form.file_field :picture %>
  </div>

  <!--
  <div class="field">
    <%= form.label :best %>
    <%= form.check_box :best %>
  </div>
  -->

  <div class="actions">
    <%= form.submit'返信する' %>
  </div>
<% end %>
